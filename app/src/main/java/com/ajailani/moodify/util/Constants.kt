package com.ajailani.moodify.util

class Constants {
    object DataStore {
        const val PREFERENCES_NAME = "moodifyPreferences"
        const val ACCESS_TOKEN_KEY = "accessToken"
    }

    object NotificationChannel {
        const val FCM_ID = "pushNotificationChannel"
        const val FCM_NAME = "Push Notification"
        const val FCM_DESCRIPTION = "Receive a push notification"
    }
}